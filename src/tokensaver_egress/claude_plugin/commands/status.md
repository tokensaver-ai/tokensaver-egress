---
description: Show TokenSaver route status, model, and Flux IA console link
---

Run:

```bash
tokensaver status
```

For a one-line statusline:

```bash
tokensaver status --line
```

Report the mode (saas/local), active model/profile, whether filesystem MCP gateway is enabled, and the Flux IA URL so the user can open the governed pipeline.
