"""
TokenSaver egress — client-side HTTPS capture proxy (ACP-4).

Published on PyPI as ``tokensaver-egress``; installs the ``tokensaver-egress`` console command.
Runs on the client (Claude Code, Cursor, n8n, …) and ships metadata / optional bodies
to the TokenSaver control plane ingest API.
"""

__version__ = "0.1.16"
